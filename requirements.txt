# Grounding layer (macOS accessibility + screen capture)
pyobjc-framework-ApplicationServices>=10.0
pyobjc-framework-Quartz>=10.0
Pillow>=10.0

# Extraction layer
pydantic>=2.0
python-dotenv>=1.0
openai>=1.0
anthropic>=0.40

# Tests
pytest>=8.0

# External binaries required on PATH (install with Homebrew, not pip):
#   brew install tesseract cliclick
